name: Go

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  call-workflow:
    uses: baalimago/repeater/.github/workflows/go.yml@main
    # Touching file a little, will then remove this
